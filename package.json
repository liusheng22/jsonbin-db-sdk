{
  "name": "@llius/jsonbin-db",
  "version": "0.0.3",
  "description": "将jsonbin.org作为数据库，提供增删改查的能力",
  "author": "liusheng",
  "license": "ISC",
  "keywords": [
    "jsonbin",
    "database",
    "json",
    "db",
    "jsonbin.org"
  ],
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "cd packages/core && pnpm build",
    "publish": "pnpm --parallel -r run publish",
    "clean": "rm -rf dist"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "@types/node": "^20.10.5",
    "tsc-alias": "^1.8.8",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@sujian/eslint-config": "^1.0.4",
    "eslint": "^8.56.0"
  }
}
